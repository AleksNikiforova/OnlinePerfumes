@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model IEnumerable<OnlinePerfumes.Models.Category>
@{
    ViewBag.Tittle = "Category List";
}
<h2>Category List</h2>

<div>
    <a href="/Category/Add" class="btn btn-primary">Add New Category</a>
</div>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Name</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var category in Model)
        {
            <tr>
                <td>@category.Id</td>
                <td>@category.CategoryName</td>
                <td>
                    <a type="button" class="btn btn-success" asp-action="Update" asp-controller="Category" asp-route-id="@category.Id">Update</a>
                    <form method="post" asp-controller="Category" asp-action="Delete">
                        <button type="submit" class="btn btn-danger" asp-action="Delete" asp-controller="Category" asp-route-id="@category.Id">Delete</button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
